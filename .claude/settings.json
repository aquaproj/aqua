{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "^(Edit|Write|MultiEdit)$",
        "hooks": [
          {
            "type": "command",
            "command": "cmdx fmt && go vet ./... && go test -race -covermode=atomic ./... && golangci-lint run"
          }
        ]
      }
    ]
  }
}
