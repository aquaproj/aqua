registries:
- type: standard
  type: standard # duplicated keys
  ref: v3.45.0 # renovate: depName=aquaproj/aqua-registry

packages:
- name: kubernetes-sigs/kind # http package, raw format
  registry: standard
  registry: standard # duplicated keys
  version: v0.15.0 # renovate: depName=kubernetes-sigs/kind
